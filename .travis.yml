matrix:
  include:
    - language: python
      python: 3.6.7
      before_install:
        - cd webserver
      install:
        - pip install -r requirements.txt
      script:
        - cd webserver && pytest

notifications:
  email:
    - pierre.thevenet@protonmail.com
    - philippe.weier@epfl.ch
